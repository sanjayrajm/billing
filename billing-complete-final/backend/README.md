# Backend (Node + Express + SQLite)
## Setup
1. cd backend
2. cp .env.sample .env  (edit JWT_SECRET)
3. npm install
4. npm run create-admin
5. npm run dev

## Notes
- API protected by JWT. Login at POST /api/auth/login
